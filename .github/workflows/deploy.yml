name: Legacy client deployment retired

on:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Confirm retirement
        run: echo "Legacy client deployment is retired; no secret is injected or published."
